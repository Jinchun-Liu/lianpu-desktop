# 联铺界面决策 · 0.3

本阶段执行用户最新授权的白、黑、黄 Shadcn Admin 风格定制，立即开展前端重构。该授权解除旧版“全部功能完成后再改前端”的顺序限制；既有业务验收要求、真实平台能力边界和本机数据保护要求继续有效。此前橄榄色原创界面属于 0.1–0.2 历史版本，本文件记录 0.3 当前实现。

## 工作场景与信息层级

使用者是白天明亮书桌前的独立资料卖家，连续核验订单、库存和领取内容。界面围绕当前账号的待办展开，以工作列表和事实详情组织操作。工作台展示真实快照计数及可追溯的待办；所有统计、账号、成员和消息均来自现有业务控制器，不使用模板样例数据。

外壳使用侧栏、顶部导航、工作区筛选与主内容四层结构。16 个工作区按日常经营、经营管理和工作区设置分组。折叠侧栏保留图标、可访问名称和鼠标提示；面包屑解释当前位置，Ctrl+K 提供键盘导航入口。本机身份及锁定操作位于侧栏底部，工作区与账号筛选保持独立、可见。默认进入工作台。

“交付凭条”继续区分付款事实、资料消息事实、平台交易事实。详情中的唯一编号可断行，编辑操作不压缩；资料全文保留换行。原有预览、核验、失败原因、权限限制与未接入说明保留，保存本机配置不会表述为平台成功。

## 色彩、排版与交互

浅色为默认外观；白色用于正文、侧栏和弹窗，黑色承担正文与导航，黄色只用于主操作和当前选中状态。颜色来自用户提供的视觉基线，属于近似定制值，不宣称第三方官方品牌色。

| 语义 | 浅色值 |
| --- | --- |
| 背景、表面、侧栏 | `#FFFFFF` |
| 主要文字 | `#171717` |
| 次要文字 | `#666666` |
| 边框 | `#E5E5E5` |
| 表头与悬停表面 | `#F7F7F7` |
| 主操作、悬停 | `#FFE60F`、`#F2D900` |
| 选中表面 | `#FFF9CC` |

黄色按钮使用黑字；危险、警告、成功和信息状态分别配有文字及独立语义色，不能只靠颜色区分。深色外观使用另一套完整的表面、文字、边框和状态变量，主操作仍使用黄底黑字。主题与侧栏状态属于本次界面偏好，不写入业务凭据。

字体使用 Windows 系统中文字体栈，正文 14px，页面标题约 23–25px，数字使用等宽或等宽数字特性。内容采用小幅圆角、细边框与紧凑行距；表格优先保留可读列宽，超宽表格只在自身容器内横向滚动。页面标题下说明业务范围，不使用营销标语或装饰性大图。

按钮、输入、分页、排序和弹窗共享视觉语义。键盘焦点具有可见轮廓；加载、错误、空态、禁用及权限原因均能阅读。原生 dialog 与独立行为模块处理焦点返回、未保存内容确认和命令菜单。短过渡仅用于状态反馈，并遵循系统“减少动态效果”设置。页面不依赖远程字体、图片或模板图标。

## 响应式结构

目标窗口为 900×620、1280×800、1440×920。侧栏可折叠；小窗口缩减顶部次要文案，保留通知、主题、筛选和刷新操作。订单表与详情在窄窗口上下排列，详情标题与长 ID 保持独立宽度。消息页随空间调整会话列表和关联栏；设置导航在更窄宽度转为横向导航。长表单、资料条目、批量结果、通知、更新和收货后服务均沿用同一套容器及输入规则。

## 来源与实现边界

仅选择性适配已获授权的官方 `satnaing/shadcn-admin` 固定提交 `e16c87f213a5ba5e45964e9b67c792105ec74d26`（参考版本 2.2.1）。参考为 MIT，Copyright (c) 2024 Sat Naing；基础组件的上游许可链另行保留。来源摘要见 `compliance/ui-reuse/origin.json`，许可原文见 `compliance/ui-reuse/licenses/`。未运行模板、安装其依赖、引入其认证组件或读取演示业务数据。

| 本地文件与范围 | 上游文件（均位于参考仓库 `src/`） | 适配方式及改动 |
| --- | --- | --- |
| `src/renderer/ui-components.js` 按钮、徽标 | `components/ui/button.tsx`、`badge.tsx` | 源码语义转写。将 variant、size、data-slot、焦点和收缩约束改为原生 HTML 帮助函数；不依赖 React、Radix 或 CVA。 |
| 同模块工作区外壳 | `components/layout/authenticated-layout.tsx`、`app-sidebar.tsx`、`header.tsx`、`main.tsx`、`nav-group.tsx`、`nav-user.tsx`；`components/search.tsx` | 源码结构转写。选取 Header / Content / Footer / Inset、分组导航、当前成员与搜索入口，改接联铺本机状态和现有动作；去掉模板路由、Cookie、演示身份及头像。 |
| `src/renderer/styles.css` 基础控件与主题 | `styles/theme.css`；`components/ui/button.tsx`、`badge.tsx`、`input.tsx`、`textarea.tsx`、`table.tsx`、`dialog.tsx`、`sheet.tsx` | 源码语义转写。将语义变量、控件变体、表格容器和 Dialog / Sheet 层次改为独立 CSS；用户色值、中文排版、业务行与窗口断点重新实现，不复制 Tailwind 构建产物。 |
| 同模块空态、页面标题、面包屑、SVG；样式中的业务布局 | 无对应上游源码 | 本项目新实现。简单几何图形自行定义，无 Lucide、Tabler 或其他图标素材复制。 |

`ui-components.js` 仅生成界面标记，公开 `escape`、`button`、`badge`、`empty`、`heading`、`shell`；业务字段转义。受信任的项目标记由现有控制器传入，该模块没有 IPC、网络或持久化能力。主控制器、页面业务视图和行为模块的其他参考关系由各自源码及统一复用清单记录。本阶段保留 vanilla JavaScript，不引入 React、Tailwind、Clerk 或新运行依赖。

## 验证口径

当前初稿已在真实 Electron 隔离工作区通过 11 项回归，证据为 `evidence/ui-redesign-2026-09-11T03-24-42-095Z/report.json`。随后依据截图修正工作台统计纵排和整理入口两处布局；初稿结果不能直接代表最终样式或发行包。行为模块 7 项、原生退出保护 5 项、当前成员权限变化 4 项另有局部证据，完整 UI01–UI20 与 F01–F60 映射及限制见 `docs/ui-migration.md`。

最终构建验证单独记录于 `release/evidence/ui-release-validation-0.3.0.json`，本文不提前声明通过。实际 Windows 系统缩放和真实中文输入法未执行。旧版本结果不能替代 0.3 新样式验证；页面存在入口也不代表外部真实能力已实现，所有结论须对应实际源码摘要和测试证据。
